  Modified disk storage write interface to mutate keys rather than splitting mutations into individual inserts and deletes. This interface change resulted in a various compile time errors that still need to be fixed.  