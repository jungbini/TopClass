  [hg] tests fix  * waitFor changelist manager sub processes finished; * Future.cancel do not interrupt sub processes execution, it only deletes task from queue if it is not running now; * to avoid code crutches it's better to wait instead of trying interrupt&cancel instantly; * in production we have to run all sub processes with indicator registered to project dispose event and wait in appropriate processHandler several ms to check indicator, then dispose process if needed; * Note: thread interrupt doesn;t work properly because BaseOsProcessHandler waitFor(timeout) uses 2 