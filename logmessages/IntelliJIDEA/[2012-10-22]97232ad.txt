  [git] Create fair GitRemoteBranches, read repository data in fixed order.  Read repository data in the following order: 1. read .git/config, parse ini to structures inside the GitConfig class. 2. parse GitRemotes, return them to GitRepository, store in a field. 3. read references (GitRepositoryReader), and create fair GitRemoteBranch instances using the supplied GitRemotes. 4. parse tracking information from GitConfig using the full-formed branches collection (not used yet).  Details: * GitConfig: store the data read from the config file in internal structures. Parse on request. Parse tracking information with the help of passed GitBranches. * Store GitRemotes and GitBranchTrackInfos in the GitRepositoryImpl, and get them from it. Therefore remove getConfig(). * Introduce GitBranchUtil.parseRemoteBranch to parse Git[Svn]RemoteBranch and reuse it in GitRepositoryReader, and in the LowLevelAccessImpl (deprecated old-style usage, but still). * Fix GitRepositoryReaderTest, make it GitLightTest, adjust config according to new algorythm which ignores remote branches without corresponding remotes.  