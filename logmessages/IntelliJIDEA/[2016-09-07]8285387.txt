  "Show command line afterwards breaks input during run and debug (PY-20651, PY-20696)  Sys.stdin should be patched in that case too like we do it in debug console, in order to notify Java side about waiting for user's input. Also if debug console was turned off and user entered input, we don't know anything about that and we're still waiting for the input and show incorrect prompt. We need to send notification if input was received and update debug console's state.  