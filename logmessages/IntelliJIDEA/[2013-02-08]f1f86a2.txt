s in HgCommitCommand is Empty. Need to commit all files in changeList,but  can't call setFiles() method.  see HgCheckinEnviroment->commit() method *Check if filePaths is empty, then  try to commit without chunk. *May produce ShellCommand error if there are many merged files. Need to fix this case.  