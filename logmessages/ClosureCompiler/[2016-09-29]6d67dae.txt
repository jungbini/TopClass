  Add a suggested fix for variables referenced before they're declared.  The fix is pretty naive: It just inserts a var statement. So:  if (x) return true; ... var x;  becomes  var x; if (x) return true; ... var x;  Ideally we'd also eliminate the later var statement, but since the ErrorToFixMapper doesn't have a NodeTraversal and Scope, that would be tricky to do. So, the fix we have here gets rid of the error but creates a 