  C/C++ compile: don't hang build on internal errors  Summary: In rare situations when something has triggered an internal error to be thrown from inside the C/C++ step, which doesn't kill the running process, it's possible for the step to deadlock.  This happens if an error is triggered before we start and finish reading all the stdout coming from the preprocess step, in which case the process itself hangs trying to write stdout and Buck gets stuck trying to finish reading from stderr inside the `close` method of the corresponding `FunctionLineProcessorThread`.  This 