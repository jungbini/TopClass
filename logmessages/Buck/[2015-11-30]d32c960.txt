 so we can get information on the Buck side about what's happening. (From the client's perspective, all it sees is that the server is disconnecting.)  It also includes an extra flush in `NGInputStream` as a guess as to what is causing the problem.  Test Plan: buck build buck, confirm Nailgun logs in buck-out/log/buck-1.log and buck-out/log/buck-0.log  Reviewed By: Coneko  fb-gh-sync-id: 5ad9514  