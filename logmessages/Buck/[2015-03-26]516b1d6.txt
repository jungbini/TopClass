  Big Buck perf fix [2/2]: Switch AppleCxxPlatform from HashedFileTool to VersionedTool  Summary: @andrewjcg and I discovered today that Apple builds are extremely slow, because `AppleCxxPlatform` uses `HashedFileTool` for `clang` and other tools.  This causes `RuleKey` generation to repeatedly hash the `clang` binary over and over. This binary is 50 MiB or more in size, which makes builds very slow.  This 