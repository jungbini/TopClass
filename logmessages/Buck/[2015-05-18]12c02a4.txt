  Make proguard config, assets dirs, and resources dirs `SourcePaths`  Summary: Previously, these were regular `Path` objects.  However, the AAR support has been generating these components and plugging them into the rest of the build which means we're incorrectly eagerly hashing their contents in rule key calculations (meaning `buck targets --rule_key` won't work with these targest).  This 