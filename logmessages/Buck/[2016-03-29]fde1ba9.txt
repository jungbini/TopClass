which fixes creation of zip files to handle symlinks properly (store a flag that it is a symlink and store its target), but the code in `com.facebook.buck.zip.Unzip` does not handle the flag. Although rules should not have symlinks in their outputs, we should first enforce that before we make this change (or fix `Unzip` to handle symlinks). Otherwise building rule locally could produce a 