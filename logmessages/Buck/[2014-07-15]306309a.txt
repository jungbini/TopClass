  Ensure generated Xcode schemes contain all actions with configuration names  Summary: Previously, the Xcode schemes generated by Buck were missing two actions (Analyze and Archive) which Xcode generates by default.  This wouldn't be a huge deal, but xctool hard-codes these entries, so we might as well include them.  In addition, Xcode specifies a configuration name for each action, defaulting to "Debug" and/or "Release". However, we will need to override these for Wilde and other projects which have "Profile" and other custom config names, so this 