  android_resources: add symbol files rules to deps  Summary: We recently fixed a bug where the `SourcePath`s wrapping the symbol files from deps weren't being added to the rule key.  This fix, however, exposed another issue where we weren't always making the rules producing these symbol files first-order deps of the android resource rule.  Test Plan: CI  Reviewed By: Coneko  fb-gh-sync-id: 3c74d8c  