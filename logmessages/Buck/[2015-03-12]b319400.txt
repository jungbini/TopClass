  [Easy] Remove AuditTestsOptions  Summary: My original take on `audit tests` was to include flags for each set of tests that the user might want to see. So to see tests for all of the dependencies of a target use the `--include-dependencies` flag, and to see all the tests for rules that include the target you use `--include-owners`. @coneko pointed out that adding these flags to each every audit command wasn't a great solution, and instead suggested I do something composable. I updated the 