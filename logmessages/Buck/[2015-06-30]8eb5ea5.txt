  CachigBuildEngine: fix re-caching bug with input-based rule keys  Summary: If we get a cache miss with the main rule key, but get a hit with the input-based rule key, we re-cache the fetched artifact using the main rule key, so that subsequent access can get a hit without resorting to calculating an input-based rule key.  This 