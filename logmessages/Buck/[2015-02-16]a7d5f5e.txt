  Implement prefix_header for cxx and apple rules  Summary: Add `prefix_headers` parameter to cxx rules and delegate the existing `prefix_header` parameter of the apple rules to this one, so both cxx and apple rules can now specify headers to be automatically included before all compiled files.  Test Plan: * `buck test`. * Compile an apple target that uses `prefix_header`.  