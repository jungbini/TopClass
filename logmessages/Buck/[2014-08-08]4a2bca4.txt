  Improve Javadoc generation to link to Android docs and fail on errors.  Summary: We recently introduced new Java code that references Android classes in its Javadoc. It turns out that linking to Android docs is non-trivial because d.android.com does not include the standard `package-list` file. Instead, Javadoc's `-offline` option must be used so that things are linked correctly.  Because Buck developers are not required to have the Android SDK installed in order to develop, this 