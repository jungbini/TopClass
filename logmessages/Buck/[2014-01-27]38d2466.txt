  Add some fields to `DexSplitMode`.  Summary: Previously, `primary_dex_patterns`, `linear_alloc_hard_limit`, and `primary_dex_classes_file` were all separate parameters to `AndroidBinaryRule`.  However, they are all related to dex splitting, so it makes sense to group them all with `DexSplitMode`.  This will also make it easier to break dex splitting out into a separate rule.  Test Plan: Built FB4A debug and release before and after this change.  Compared the verbose RuleKeys and only saw the prefixes and missing `primaryDexClassesFile` as 