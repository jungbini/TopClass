  ShBinary: don't alter `PWD` env var in bootstrap script  Summary:Previously, the `sh_binary` wrapper script would temporarily change directories to setup symlinks, then change back before running the real script.  This had the side-effect of normalizing the `PWD` env var, which meant our debug path sanitization strategy for the `combined` preprocess mode wouldn't work when using tools behind a `sh_binary` (since we need to pad the trailing `/` chars).  This 