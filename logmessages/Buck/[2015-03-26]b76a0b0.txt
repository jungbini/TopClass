  Big Buck perf fix [1/2]: Add Xcode version to AppleSdk  Summary: @agallagher and I discovered today that Apple builds are extremely slow, because `AppleCxxPlatform` uses `HashedFileTool` for `clang` and other tools.  This causes `RuleKey` generation to repeatedly hash the `clang` binary over and over. This binary is 50 MiB or more in size, which makes builds very slow.  We don't cache it because we can only cache hashes for files under Watchman (or else the caches would get stale), which doesn't include Xcode utilities.  This 