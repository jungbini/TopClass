  More Apple watch fix  Summary: Besides the `_WatchKitStub/WK` in the app bundle, Apple also wants `Symbols' (empty) and `WatchKit/WK` (a copy of the `_WatchKitStub/WK` file inside the app bundle) alongside the `Payload` directory of the `.ipa` file.  Test Plan:CI  * Ensure we can install a sample app on a device + watch * Ensure we can submit a IPA to Apple without being auto-rejected  Reviewed By: beefon  fb-gh-sync-id: a2c3974 shipit-source-id: a2c3974  