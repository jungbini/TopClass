  go: fix rule keys  Summary: Go binary rules auto-generate a main module by extracting a built-in Buck resource.  This resource would be extracted to a new temp file at action graph creation time and wrapped in a `PathSourcePath`. However, since the temp file had a non-deterministict path, this would get hashed into the rule key, meaning the rule keys would change every time the action graph was re-created.  This 