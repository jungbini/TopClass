  Fix 'buck build //path/to:cxx_library' so it no longer silently does nothing  Summary: Currently, `buck build //path/to:cxx_library` currently silently does nothing.  This is because `CxxLibraryDescription` returns no-op placeholder build rules so that graph enhancement can later fill in (via flavors) the platform and link type.  This unfortunately means that `buck build //path/to:cxx_library` also uses that no-op placeholder build rule and does nothing.  In this 