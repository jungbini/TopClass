  Revert language level and use source level as fallback  Summary: `language_level` options was replaced with `source` option, but that doesn't work with projects that use retralambda because of some issues with dx when global source level is set 8.  This fix adds `language_level` back, but leaves `source` as fallback path.  Test Plan: Integration tests.  Reviewed By: JonShemitz  fbshipit-source-id: 78ed3e3  