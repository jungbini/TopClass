  Fix `CacheCommand` default constructor.  Summary: af0c188 made our `ArtifactCache` implementations take an instance of `FileHashCache` to be constructed, which required threading through a copy the file cache into `CacheCommand`.  That change did this via adding a new `FileHashCache` parameter to it's constructor which violates how `Command` subclasses work: they should only take a single `CommandRunnerParams` argument.  This 