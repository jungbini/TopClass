  various Sonar plugin improvements  - get rid of bootstrapDir config property because Sonar doesn't allow to set it anymore - don't set a default for sonar.projectDate because Sonar already provides the same default - fixed module prefix for sonar properties - use a unique sonar.projectKey, and URL-encode it (because Sonar uses it in URL query parameters) - set sonar.environment properties - use correct key for libraries  