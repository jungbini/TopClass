  Unbreak timestamp deserializing in the message cache and add tests.  A missing manual conversion of the serialized timestamp (a long) to a DateTime object in the deserialize method caused the timestamp to be stored as a long in Elasticsearch. This wasn't catched by static typing because the Message object stores all fields in a "Map<String, Object>" and Jackson happily deserialized the field into a long.  The current Message implementation also does not allow to set the "_id" field. That means there was no "_id" field in the deserialized Message. The deserializer now creates the Message object in a 