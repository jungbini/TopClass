  Allowing path prefixes in web_listen_uri so web interface is accessible via path != "/" (#2440)  * Prepare JS part for application prefix.  * Add web prefix config variable and using it.  * Prefixing assets in index.html properly.  * Prefixing assets in index.html template properly.  * Introducing `web_prefix` parameter.  * Prefixing jersey resources, redirecting for index if no slash  Export web app prefix in generated `config.js`  * Using correct web app uri prefix in all places.  * Making things way easier. Using web_listen_uri for configuration now.  * Adapting sample config to allowing path prefixes in web_listen_uri.  * Removing comment.  * Removing now unused validator.  * Prefixing favicon properly.  * Preventing double prefixing for plugin assets.  * Prefixing navigation items provided by plugins properly.  * Prefixing plugin routes in app router.  