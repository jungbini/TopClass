 do not ignore the expected response codes in executeOnAll  instead, it now throws an exception if any of the parallel requests failed due to an unexpected return code (e.g. 401) which gives the caller the opportunity to return an error code to its caller (usually the UI) extracted common code between execute() and executeOnAll() to reduce the fragility of the latter copied missing test cases from the web interface repository  fixes Graylog2/graylog2-web-interface#1205  