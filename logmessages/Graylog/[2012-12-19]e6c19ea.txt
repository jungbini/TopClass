  let inputs decide what to do when buffers are full  also introduces waiting for buffer capacity in AMQP input. could be added to HTTP inputs, too. giving back a reasonable HTTP status code when out of capacity. (cc @kroepke)  waiting for capacity code duplication in AMQPConsumer sucks and needs to be reduced by using interfaces for the GELF and syslog processors. (or something similar.)  fixes #SERVER-247  