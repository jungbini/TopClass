  first changes for 0.20.0  everthing is now prepared to accept messages from all source with all kinds of data. we are no longer limited to log data. host is source now. everything except source and message is optional and additional fields don't exist anymore because now everything except the two standard fields is an additional field. severity is renamed to level, underscore prefix for additional fields is gone. also introducing the new stream rule model: it does not matter on which fields you apply a rule now. the rules only 