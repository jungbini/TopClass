  use explicit order for message filters  each message filter now has a priority value, according to which the order of the filters is determined. lower values mean that a filter is run earlier. the defaults are set by the following rule: if the filter creates new fields it should be run early blacklist runs after all new fields have been added stream matching happens last  added test for ServerProcessBufferProcessor  fixes #601  