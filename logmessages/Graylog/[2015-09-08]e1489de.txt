  ensure that alarm callback configurations conform to the declared types in the requested plugin config  - the web interface had a bug where integers were treated as strings on updates causing the wrong config to be stored - also fix an NPE that could happen with exceptions having a null message - remove AlarmCallbackConfigurationService#update in favor of load/update object/save to allow for sanitizing the configuration - removed various author javadocs which we don't use in our codebase  fixes #1399  