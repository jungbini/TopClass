  Adjust several helper and REST resources to index sets (#3121)  * Adjust several helper and REST resources to index sets  - Add `get()` and `getForIndexName()` methods to IndexSetRegistry - Adjust all methods in Indices to index sets - Add `indexAnalyzer()`, `indexTemplateName()`, `indexOptimizationMaxNumSegments()` and `indexOptimizationDisabled()` methods to IndexSetConfig (only static for now, needs migration) - Adjust DeflectorResource to index sets - Adjust IndexerOverviewResource to index sets - Adjust IndicesResource to index sets  * Add missing rotation/retention strategy class names to IndexSetConfig  * We need one index template per index set because of 